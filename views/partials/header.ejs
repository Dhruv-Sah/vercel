<!DOCTYPE html>
<html>
	<head>
		<title>YelpCamp</title>
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
		<link rel="stylesheet" href="/stylesheets/main.css">
		</head>

	<body>
		<nav class="navbar navbar-light bg-light navbar-expand-lg mb-3" >
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
				<span class="navbar-toggler-icon"></span>
			  </button>
		<a href="/campgrounds" class="navbar-brand">HOME</a>	
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav">
					<li class="nav-item dropdown">
						<a  class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" >
							Category				
						</a>
						<div class="dropdown-menu" aria-labelledby="navbarDropdown">
						  <a class="dropdown-item" href="campgrounds/trek">Trek</a>
						  <a class="dropdown-item" href="/campgrounds/nontrek">Non-Trek</a>
						</div>
					</li>
					<% if(currentUser && currentUser.admin){ %>
					<li class="nav-item">
						<a href="/data" class="nav-link">UserData</a>
					</li>
					<% } %>
			</ul>
	
				<ul class="navbar-nav ml-auto">
					<%	if(!currentUser) { %>
							<li class="nav-item"><a href="/login" class="nav-link">Login</a></li>
							<li class="nav-item"><a href="/register" class="nav-link">SignUp</a></li>			
					<%	}else{ %>
						<% if(currentUser.admin){ %>
							<li class="nav-item"><a class="nav-link"  href="#">(Admin)</a></li>
						  <% } %>	
							<li class="nav-item mr-0 pr-0"><a href="#" class="nav-link mr-0 pr-0"><i class="fa fa-user"></i></a></li>
							<li class="nav-item dropdown">					
								<a  class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" >
									<%= currentUser.username %>					
								</a>
								<div class="dropdown-menu" aria-labelledby="navbarDropdown">
								  <a class="dropdown-item" href="/<%= currentUser._id %>/essentials">Camp-Essentails</a>
								  <a class="dropdown-item" href="/<%= currentUser._id %>/bucketlist">Bucket List</a>
								  <a class="dropdown-item" href="/<%= currentUser._id %>/edit">Update Profile</a>
								  <div class="dropdown-divider"></div>
								  <p class="dropdown-item lead">Happy Camping</p>
								</div>
							</li>
							<li class="nav-item"><a href="/logout" class="nav-link">LogOut</a></li>		
					<%	} %>
					</ul>		
			
		</nav>
	</div>

		<div class="container text-center">
			<% if(error && error.length>0){ %>
				<div class="alert alert-danger">
					<%= error %>
				</div>
			<% } %>
			<% if(success && success.length>0){ %>
				<div class="alert alert-success">
					<%= success %>
				</div>
			<% } %>	
		</div>
		


		